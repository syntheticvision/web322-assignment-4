<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Collection - Lego Sets</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body class="bg-gray-100">
    <%- include('partials/navbar', { page: '/lego/sets' }) %>

    <div class="container mx-auto">
        <div class="hero bg-blue-500 text-white p-6 rounded-lg my-6">
            <h1 class="text-3xl">Collection</h1>
            <p class="mt-2">Explore the amazing collection of Lego sets!</p>
            <div class="mt-4">
                <a href="/lego/sets?theme=Technic" class="btn btn-primary m-1">Technic</a>
                <a href="/lego/sets?theme=Classic%20Town" class="btn btn-primary m-1">Classic Town</a>
                <a href="/lego/sets?theme=Space" class="btn btn-primary m-1">Space</a>
            </div>
        </div>

        <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-200">
                <thead>
                    <tr>
                        <th class="py-2 px-4 border-b">Image</th>
                        <th class="py-2 px-4 border-b">Name</th>
                        <th class="py-2 px-4 border-b">Theme</th>
                        <th class="py-2 px-4 border-b">Year</th>
                        <th class="py-2 px-4 border-b">Parts</th>
                        <th class="py-2 px-4 border-b">Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% sets.forEach(set => { %>
                    <tr>
                        <td class="py-2 px-4 border-b">
                            <img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-20 h-20 object-cover rounded-full">
                        </td>
                        <td class="py-2 px-4 border-b"><%= set.name %></td>
                        <td class="py-2 px-4 border-b">
                            <a href="/lego/sets?theme=<%= set.theme %>" class="btn btn-primary"><%= set.theme %></a>
                        </td>
                        <td class="py-2 px-4 border-b"><%= set.year %></td>
                        <td class="py-2 px-4 border-b"><%= set.num_parts %></td>
                        <td class="py-2 px-4 border-b">
                            <a href="/lego/sets/<%= set.set_num %>" class="btn btn-secondary">Details</a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>
